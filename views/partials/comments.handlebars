<div class="comments">
  {{#each this}}
  <article class="comment">
    <div class="title">
      <h2></h2>
    </div>
    <div class="content">
      <p>{{comment_text}}</p>
      <p>- {{user.username}} on {{format_date created_at}}</p> 
    </div>
  </article>
  {{/each}}
</div>